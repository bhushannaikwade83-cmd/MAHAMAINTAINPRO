class RouteNames {
  RouteNames._();

  static const splash = '/';
  static const login = '/login';
  static const otpLogin = '/login/otp';
  static const register = '/register';
  static const dashboard = '/dashboard';

  // Sprint 2+ placeholders — routed to a "coming soon" screen for now so
  // navigation/menu wiring can be built ahead of the feature itself.
  static const leads = '/leads';
  static const customers = '/customers';
  static const societies = '/societies';
  static const serviceRequests = '/service-requests';
  static const userManagement = '/admin/users';
}
